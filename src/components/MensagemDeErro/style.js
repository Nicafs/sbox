import styled from 'styled-components';

const Error = styled.h1.attrs(props => props)`
  color: red;
  font-size: 18px;
  font-weight: normal;
  letter-spacing: 0.8px;
  text-align: center;
`;

export { Error };
