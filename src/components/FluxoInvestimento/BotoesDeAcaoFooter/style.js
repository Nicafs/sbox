import Button from 'components/MaterialUI/Button';
import styled from 'styled-components';

export const BoletoAcoesButtonStyled = styled(Button)`
  border-color: ${({ theme }) => theme.palette.primary.main};
`;
